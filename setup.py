#!/usr/bin/env python3
"""Setup script for quantify package."""

from setuptools import setup, find_packages

setup(
    name="quantify",
    version="1.0.0",
    packages=find_packages(),
    python_requires=">=3.9",
)


